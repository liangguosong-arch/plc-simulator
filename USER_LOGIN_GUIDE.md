# PLC模拟器 - 用户登录功能说明

## 🎯 功能概述

Web管理界面现已支持多用户登录和权限切换功能，方便测试不同角色下的操作权限。

---

## 🔐 默认账户

| 用户名   | 密码      | 角色       | 权限说明               |
|---------|----------|-----------|----------------------|
| guest   | guest123 | GUEST     | 只读权限（默认自动登录） |
| operator| op123    | OPERATOR  | 基本读写权限           |
| engineer| eng123   | ENGINEER  | 配置+读写权限          |
| admin   | admin123 | ADMIN     | 全部权限               |

---

## 🚀 使用流程

### 1. 自动登录（访客模式）

**首次访问页面时**：
- ✅ 系统自动以 **guest** 身份登录
- ✅ 状态栏显示：`Guest (GUEST)`
- ✅ 无需手动输入任何凭据
- ✅ 可以查看变量和配置（只读）

### 2. 切换用户

**步骤**：
1. 点击页面右上角的 **"👤 Switch User"** 按钮
2. 弹出登录对话框
3. 输入目标用户的用户名和密码
4. 点击 **"Login"** 按钮
5. 登录成功后：
   - 右上角显示新用户信息
   - 页面重新加载配置（应用新权限）
   - 显示成功提示

**示例**：
```
切换到 admin 账户：
Username: admin
Password: admin123
```

### 3. 登录状态持久化

**特性**：
- ✅ 登录状态保存在浏览器 `localStorage`
- ✅ 刷新页面后自动恢复上次登录的用户
- ✅ 关闭浏览器后仍然保持登录状态
- ✅ 清除浏览器缓存会退出登录

---

## 📋 权限说明

### GUEST（访客）
- ✅ 查看设备状态
- ✅ 读取变量值
- ❌ 修改变量配置
- ❌ 写入变量值
- ❌ 执行设备命令

### OPERATOR（操作员）
- ✅ 查看所有信息
- ✅ 读取变量值
- ✅ 写入变量值（手动模式）
- ❌ 修改模拟配置
- ❌ 执行高级命令

### ENGINEER（工程师）
- ✅ 所有OPERATOR权限
- ✅ 修改变量模拟配置
- ✅ 切换Auto/Manual模式
- ✅ 配置模拟策略和参数
- ⚠️ 部分管理功能受限

### ADMIN（管理员）
- ✅ 所有权限
- ✅ 完全控制设备
- ✅ 执行所有命令
- ✅ 管理系统配置

---

## 🎨 界面说明

### 右上角用户信息区

```
┌─────────────────────────────────────┐
│  [User Name]                        │
│  [Role]          [👤 Switch User]   │
└─────────────────────────────────────┘
```

**显示内容**：
- **User Name**: 当前登录用户名
- **Role**: 用户角色（大写）
- **Switch User按钮**: 点击打开登录对话框

### 登录对话框

```
┌──────────────────────────────┐
│       🔐 User Login          │
│ Enter your credentials...    │
│                              │
│ Username: [____________]     │
│ Password: [____________]     │
│                              │
│      [    Login    ]         │
│      [   Cancel   ]          │
└──────────────────────────────┘
```

**功能**：
- 输入用户名和密码
- 错误时显示红色错误提示
- 登录中按钮显示"Logging in..."并禁用
- 点击遮罩层或Cancel取消登录

---

## 💡 使用场景

### 场景1：快速查看（默认）
```
1. 打开 http://localhost:8080
2. 自动以guest身份登录
3. 浏览变量和配置
4. 无需任何操作
```

### 场景2：测试变量写入
```
1. 点击右上角 "Switch User"
2. 输入：operator / op123
3. 登录后切换到Manual模式
4. 修改变量值并保存
```

### 场景3：配置模拟策略
```
1. 切换到 engineer 或 admin
2. 选择Auto模式的变量
3. 修改Strategy、Range、Min/Max等参数
4. 点击 "Save All Configuration"
```

### 场景4：测试权限控制
```
1. 以guest身份尝试修改变量 → 应该失败
2. 切换到operator → 可以写入但不能改配置
3. 切换到engineer → 可以修改配置
4. 验证不同角色的权限边界
```

---

## 🔧 技术实现

### 前端
- **自动登录**: `mounted()` 钩子调用 `autoLoginAsGuest()`
- **Token存储**: `localStorage.setItem('plc_simulator_token', token)`
- **用户信息**: `localStorage.setItem('plc_simulator_user', JSON.stringify(user))`
- **请求认证**: 所有API请求添加 `Authorization: Bearer {token}` 头

### 后端
- **可选认证**: 使用 `optionalAuth` 中间件
- **Token验证**: JWT验证，提取用户角色
- **权限检查**: 根据角色决定是否允许操作

### 数据流
```
用户登录 
  ↓
获取JWT Token
  ↓
保存到localStorage
  ↓
后续请求携带Token
  ↓
后端验证Token
  ↓
返回相应数据
```

---

## ⚠️ 注意事项

1. **本地开发环境**: 当前为简化开发，API采用可选认证，即使没有Token也能访问
2. **生产环境**: 建议启用强制认证，保护敏感操作
3. **密码安全**: 默认密码仅用于开发测试，生产环境应修改
4. **会话管理**: 当前无Token过期处理，24小时后需重新登录
5. **多标签页**: 多个标签页共享同一登录状态

---

## 🐛 故障排查

### 问题1：登录后仍显示Guest
**原因**: Token未正确保存  
**解决**: 
1. 打开浏览器控制台（F12）
2. 检查是否有JavaScript错误
3. 确认localStorage中有 `plc_simulator_token`
4. 刷新页面重试

### 问题2：无法切换到其他用户
**原因**: 用户名或密码错误  
**解决**:
1. 检查用户名拼写（全小写）
2. 确认密码正确
3. 查看登录对话框中的错误提示
4. 参考上方的默认账户表

### 问题3：刷新后需要重新登录
**原因**: localStorage被清除  
**解决**:
1. 不要清除浏览器缓存
2. 检查浏览器隐私设置
3. 确认未使用无痕模式

---

## 📝 API端点

### 登录接口
```http
POST /api/v1/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "userId": "admin",
    "role": "ADMIN",
    "name": "Administrator"
  },
  "message": "success",
  "timestamp": 1712649600000
}
```

---

## 🎉 总结

✅ **自动登录**: 访客模式，无需操作  
✅ **一键切换**: 右上角按钮快速切换用户  
✅ **状态持久**: 刷新页面保持登录  
✅ **权限分级**: 4种角色满足不同需求  
✅ **友好提示**: Toast消息实时反馈  

现在您可以轻松地在不同用户角色间切换，测试各种权限下的功能！🚀
